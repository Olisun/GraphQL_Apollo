import React, { Component } from 'react';

import { Card } from "react-bootstrap";

export default function LaunchItem({
  // Destructuring inside the args
  launch: { flight_number, mission_name, launch_date_local }
}) {
  return (
    <div>
      <p>test</p>
    </div>
  )
}